import React, { Component } from 'react';

class UIHeader extends Component {
  constructor( props ) {
    super( props );
  }

  render() {
    return (
      <div>Header!!</div>
    );
  }
}

export default UIHeader;
